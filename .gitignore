*.iml
.gradle
.key
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/google-services.json

# Signed build outputs produced by Android Studio's release wizard
/app/release/
*.apk
*.aab

# Ad-hoc screenshots dropped in the repo root (graph/ keeps the curated ones)
/Screenshot_*.png

# Working notes, not part of the project's public documentation
/docs/code-quality-suggestions.md

# Claude Code: the per-machine permission set. A shared .claude/settings.json
# is versioned deliberately if one is ever added; this one never is.
/.claude/settings.local.json
